-- ============================================================
-- עוגן פליי — שיוך ידני של אפליקציה/תוכנה ל"אנדרואיד" או ל"תוכנות"
-- בנוסף לסיווג האוטומטי לפי סיומת הקובץ (components/AppGrid.tsx: isApk) - לצוות פיקוח
-- ומנהל, למקרים חריגים שבהם הסיווג האוטומטי לא מתאים (למשל קובץ עם סיומת לא סטנדרטית).
-- null = סיווג אוטומטי (ברירת המחדל, כרגיל).
-- הריצו את כל הקובץ הזה ב-Supabase Dashboard -> SQL Editor -> New query (או psql בשרת)
-- ============================================================
alter table public.apps add column if not exists platform_override text check (platform_override in ('apk','software'));
